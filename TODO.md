# TODO

> 当前代码是事实标准。本文件只记录尚未完备、需要后续产品化或工程化补齐的事项。

## P0 交付硬化

- [ ] 前端品牌文案收敛：`WelcomeOverlay.tsx`、`WelcomeCard.tsx` 仍有旧展示文案；本轮不改代码，后续统一替换为“任意门”。
- [ ] 生产域名与密钥配置：配置 `VITE_AMAP_KEY` 域名白名单、`CORS_ALLOW_ORIGINS`、Caddy HTTPS 和真实部署域名。
- [ ] 数据库运维：补充备份/恢复流程、schema 迁移策略和生产数据初始化手册。
- [ ] 真实 AI 接入验收：为 LLM 和图像生成提供正式供应商配置、费用阈值、失败告警和灰度开关。
- [ ] 后台权限升级：`ADMIN_TOKEN` 适合单管理员轻量管理，正式交付需增加账号、会话、操作审计和最小权限。

## P1 产品补全

- [ ] 真全景/街景能力决策：当前 `StreetViewCanvas` 使用静态图片沉浸视图，需补真实全景资产与交互，或彻底移除“拖拽漫游”类文案和 Pannellum 残留依赖。
- [ ] 图片性能优化：城市封面、地标、美食、人物、勋章需 WebP/多尺寸、懒加载、占位图和缓存策略。
- [ ] 内容 CMS 完整度：后台已有城市更新与子内容增删改，但缺城市创建/删除、批量导入审核、媒体替换历史。
- [ ] 内容与评论审核：补评论敏感词/举报/删除能力，后台导入图片和 AI 输出需要审核状态。
- [ ] 用户体系增强：当前账号是简单注册/登录并沿用 user_id 调用，需增加认证 token、密码重置、头像上传和账号注销。
- [ ] 分享链接产品化：猜城市挑战当前返回相对路径，部署后需生成带真实 origin 的分享 URL，并补过期/访问统计。
- [ ] 前端自动化测试：补充关键流程的组件测试或 E2E，包括自由探索、任意门、打卡任务、成就、后台鉴权。

## P2 整理与维护

- [ ] 清理前端遗留文件：`DiceConsole.tsx`、`CityDrawer.tsx`、`pannellum-react` 依赖和 `vite-env.d.ts` 中的 Pannellum 声明。
- [ ] 统一容器与包命名：代码包名仍保留历史 `city-roam` 命名，后续可在不破坏部署的前提下逐步迁移。
- [ ] 完善观测文档：补接口日志字段、AI 用量指标、任务失败指标和后台操作审计指标。
- [ ] 建立文档审计脚本：自动检查旧项目名、旧阶段口径、失效文档引用和 seed/接口字段漂移。
